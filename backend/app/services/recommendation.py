from ..utils.text_processing import TextProcessor
from typing import List


def suggest(text: str) -> List[int]:

    # tokenize the text
    # get the embeddings
    # get the most similar books
    # return the books id

    books_id: List[int] = [1, 2, 3]

    return books_id
